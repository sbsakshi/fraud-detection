# infra

Deployment and environment configuration beyond the local `docker-compose.yml`
at the repo root (e.g. intervention-threshold config, replay-simulator
scheduling) lands here as later phases need it. Empty by design for now.
