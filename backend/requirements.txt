fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
# Pinned to match the versions ml/notebooks/model_training.ipynb trained
# against (see ml/requirements.txt) -- joblib pickles of scikit-learn/xgboost
# objects aren't guaranteed compatible across library versions.
joblib==1.6.0
scikit-learn==1.9.1
xgboost==3.4.1
# Phase 5 graph module (app/graph/).
networkx==3.6.1
# Phase 6: app/ml/feature_engineering.py (vendored from ml/features/engineering.py)
# needs pandas to compute live features the same way training did; pinned to
# match ml/requirements.txt for the same reason as the ML libraries above.
numpy==2.5.3
pandas==3.0.5
shap==0.52.0
